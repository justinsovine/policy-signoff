# App
APP_KEY=              # Generate with the command in step 2 of docs/deployment.md
DB_PASSWORD=          # MySQL root password — generate with: openssl rand -base64 24

# MinIO / S3 credentials
# MINIO_ROOT_USER and MINIO_ROOT_PASSWORD set the MinIO container's admin credentials.
# AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY are what Laravel uses to talk to MinIO.
# These MUST match — they're the same credentials used by two different services.
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
AWS_ACCESS_KEY_ID=      # Same as MINIO_ROOT_USER
AWS_SECRET_ACCESS_KEY=  # Same as MINIO_ROOT_PASSWORD
